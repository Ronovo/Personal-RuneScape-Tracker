node_modules/
*.log
dist/
# public/**/*.js is tsc output (built from src/client/**/*.ts and src/shared/**/*.ts) - html/css are hand-written and stay tracked.
public/**/*.js

# Local environment/secrets
.env
.env.*
!.env.example

# Runtime state: synced player data, watchlists, and local accounts. All of it
# lives in the Docker volume / *_DATA_DIR paths - none of it belongs in the repo.
/data/

# OS files
.DS_Store
Thumbs.db
Desktop.ini

# Tool output
coverage/
*.tsbuildinfo
tmp-layout/